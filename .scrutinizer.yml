build:
    tests:
        override:
            -
                command: 'vendor/bin/phpunit --coverage-clover=coverage-report'
                coverage:
                    file: 'coverage-report'
                    format: 'clover'